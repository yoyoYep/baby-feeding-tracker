# 宝宝喂养记录 - 微信小程序功能文档

> 参考美柚App育儿模式-喂养记录工具，适配0-12个月龄宝宝日常记录与0-24个月生长曲线场景

---

## 一、产品概述

### 1.1 目标用户
- 0-12个月婴儿的主要照顾者（妈妈/爸爸/祖辈）

### 1.2 核心价值
- 快速记录每次喂养/换尿布/睡眠等日常事件
- **语音一句话快速记录**，解放双手（腾讯云ASR语音转文字 + DeepSeek LLM 智能解析）
- 生长数据与 WHO 标准曲线对比，辅助科学育儿
- 多人协同记录，家庭成员共享数据
- 记录标记身份，区分谁做了什么
- 家庭共享喂养配置，每顿奶量和喂奶间隔阈值多成员同步生效
- 每日健康待办减少重复配置，完成后自动沉淀为正式记录
- 首页提供本地规则优先的喂奶计划，按每日顿数/每顿奶量/夜间勿扰/最短间隔动态预估下一顿
- 小程序前台提供程序内提醒：待办/用药/喂奶到点震动弹窗，支持稍后提醒和倒计时显示

---

## 二、语音识别输入（全局能力）

### 2.1 功能说明
所有记录操作均支持语音/文字输入，用户说一句话即可自动创建对应事件记录。

### 2.2 技术方案
- 语音采集：微信 RecorderManager（mp3/16kHz/单声道）
- 语音转文字：`speechToText` 云函数调用**腾讯云 ASR 一句话识别**（16k_zh引擎）
- 文字→结构化数据：通过 `parseRecord` 云函数调用 **DeepSeek Chat API** 进行自然语言理解
- 本地兜底：云函数失败时使用 `voice-parser.js` 中的正则引擎进行基础解析（覆盖全部类型）
- **解析等待态**：语音/文字输入后显示统一 loading 动画（"识别中..." → "正在理解..."），解析完成后展示结果确认弹窗

### 2.3 交互流程
1. 首页底部语音横条：**点击**开始录音，再次点击结束录音
2. 右侧键盘按钮弹出文字输入框，适合不方便说话时使用
3. 录音中显示秒数和波形动画
4. 结束录音 → 统一 loading 弹窗显示"识别中..."，ASR 完成后切换为"正在理解..."
5. 腾讯云 ASR 转为文字 → DeepSeek LLM 识别：**事件类型 + 关键数据 + 发生时间 + 动作类型**
6. 展示确认弹窗："喂奶 120ml（5月27日 14:30）"，包含**确认 / 编辑 / 取消**三个操作
7. 用户确认后提交，自动写入数据库；选择编辑则跳转记录页预填字段
8. 任何环节失败均降级到文字输入

### 2.4 语音识别规则

| 语音示例 | 识别结果 |
|----------|----------|
| "刚刚喂了120ml奶" | 喂奶记录，奶量120ml，时间=当前 |
| "10分钟前喝了90毫升配方奶" | 喂奶记录，奶量90ml，时间=10分钟前 |
| "开始喂奶" | 喂奶开始（创建ongoing状态记录） |
| "2分钟后开始喂奶" | 首页显示倒计时，到点自动创建ongoing状态记录 |
| "喂完了，喝了120ml" | 结束当前进行中的喂奶，记录120ml |
| "刚喝完奶喝了半小时" | 喂奶完整记录，开始时间=30分钟前，结束时间=当前 |
| "宝宝刚拉了大便，金黄色糊状的" | 换尿布，大便，金黄色，糊状，时间=当前 |
| "下午两点换了尿布，小便" | 换尿布，小便，时间=14:00 |
| "宝宝睡着了" | 睡眠记录，开始计时，时间=当前 |
| "宝宝醒了" | 结束当前睡眠记录，时间=当前 |
| "昨晚11点睡到早上6点" | 睡眠完整记录，支持跨天起止时间 |
| "25号早上7点喝了120ml" | 喂奶记录，120ml，时间=当月25号07:00 |
| "26号体重5公斤" | 生长记录，5kg，时间=当月26号 |
| "昨天体重5000克" | 生长记录，5kg，时间=昨天 |
| "量了体温37.2" | 体温记录，37.2°C，时间=当前 |
| "刚吃了半包蒙脱石散" | 用药记录，蒙脱石散，半包，时间=当前 |
| "吃了米粉，大概30克，挺喜欢的" | 辅食记录，米粉，30g，反应=喜欢 |
| "刚给宝宝洗澡了，水温38度" | 洗澡记录，水温38°C，时间=当前 |
| "体重5.3公斤，身高65厘米" | 生长记录，5.3kg，65cm |

### 2.5 时间识别规则
| 用户表述 | 解析为 |
|----------|--------|
| "刚刚" / "刚才" | 当前时间（now） |
| "X分钟前" | 当前时间 - X分钟 |
| "X小时前" | 当前时间 - X小时 |
| "X分钟后" | 当前时间 + X分钟（用于延迟开始喂奶） |
| "X小时后" | 当前时间 + X小时（用于延迟开始喂奶） |
| "上午/下午X点" | 今日对应时间 |
| "半小时前" | 当前时间 - 30分钟 |
| "昨天" | 昨天当前时刻 |
| "昨晚X点" | 昨日对应时间 |
| "前天" | 前天当前时刻 |
| "25号早上7点" | 当月25号07:00 |
| "3号下午2点" | 当月3号14:00 |
| "3号" / "25号" | 当月对应日期 |
| 未提及时间 | 默认当前时间 |

---

## 三、功能模块

### 3.1 喂奶记录

| 字段 | 类型 | 说明 |
|------|------|------|
| 奶量 | 数字输入 | 单位：ml，快捷选择（60/90/120/150） |
| 开始时间 | 自动/手动 | 计时模式自动记录，手动模式可选择 |
| 结束时间 | 自动/手动 | 计时模式自动记录，手动模式可选择 |
| 持续时长 | 自动计算 | 结束时间 - 开始时间 |
| 日期 | 日期选择器 | 默认今天，支持回溯记录 |
| 备注 | 文本输入 | 可选 |

**交互说明：**
- **开始/结束计时模式**：
  - 点击"开始喂奶"→ 页面进入计时状态，显示已用时间
  - 计时进行中可调整开始时间，调整后同步更新 ongoing 记录并重新计算已用时
  - 喂完点击"结束喂奶"→ 自动记录时长，弹出奶量填写
  - 填入奶量后保存，整个记录自动完成
- **语音开始/结束动作**：
  - "开始喂奶"创建进行中记录
  - "喂完了/喝完了"优先结束当前进行中记录
  - 如果语音没有说奶量，自动使用"我的 → 喂养配置"中的每顿奶量
- **手动补录模式**：
  - 选择开始时间和结束时间（均为 time picker）
  - 选择日期（date picker，支持回溯到过去的日期）
  - 填入奶量，保存记录
- 首页显示"正在喂奶中... XX:XX"状态卡片
- 奶量支持快捷按钮+手动数字输入

---

### 3.2 换尿布记录

| 字段 | 类型 | 说明 |
|------|------|------|
| 类型 | 选择 | 小便 / 大便 / 混合 |
| 大便形状 | 选择 | 水样 / 糊状 / 软便 / 条状 / 颗粒状 |
| 大便颜色 | 颜色选择 | 金黄 / 黄绿 / 绿色 / 深褐（带色块预览） |
| 大便量 | 选择 | 少量 / 适量 / 较多 |
| 时间 | 时间选择器 | 默认当前时间 |
| 日期 | 日期选择器 | 默认今天，支持回溯 |
| 备注 | 文本输入 | 可选 |

**交互说明：**
- 选择"大便"或"混合"时才显示形状/颜色/量字段
- 颜色选项带圆形色块直观展示
- 形状选项为五档，覆盖从最稀到最干的情况

---

### 3.3 睡眠记录

| 字段 | 类型 | 说明 |
|------|------|------|
| 睡眠类型 | 选择 | 夜觉 / 小睡（白天） |
| 入睡时间 | 时间选择器 | 支持计时器模式 |
| 醒来时间 | 时间选择器 | 支持计时器模式 |
| 睡眠时长 | 自动计算 | 入睡到醒来的时长 |
| 入睡方式 | 选择标签 | 奶睡 / 抱睡 / 自主入睡 / 拍睡 |
| 睡眠质量 | 选择 | 安稳 / 一般 / 易醒 |
| 夜醒次数 | 数字 | 仅夜觉时显示（0/1/2/3+） |
| 备注 | 文本输入 | 可选 |

**交互说明：**
- 支持"宝宝睡了/醒了"计时模式
- 睡眠进行中可调整入睡开始时间，调整后同步更新 ongoing 记录并重新计算已睡时长
- 首页显示"宝宝正在睡觉... 已睡 XX:XX"状态卡片
- 编辑模式支持手动选择入睡/醒来时间

---

### 3.4 辅食记录

| 字段 | 类型 | 说明 |
|------|------|------|
| 食物名称 | 标签选择/自定义输入 | 预设：米粉/南瓜泥/红薯泥/蛋黄/苹果泥/香蕉泥/胡萝卜泥/西兰花泥 |
| 食用量 | 选择 | 少量 / 适量 / 较多 |
| 宝宝反应 | 选择标签 | 喜欢 / 一般 / 不喜欢 / 过敏 |
| 日期 | 日期选择器 | 默认今天 |
| 备注 | 文本输入 | 可选 |

---

### 3.5 洗澡记录

| 字段 | 类型 | 说明 |
|------|------|------|
| 时间 | 时间选择器 | 默认当前时间 |
| 水温 | 数字输入 | 单位：°C，可选 |
| 时长 | 数字输入 | 单位：分钟，可选 |
| 备注 | 文本输入 | 可选 |

**交互说明：**
- 首页快捷操作面板中"洗澡"跳转到统一记录页
- 时间线显示为普通完成记录，描述展示水温/时长（如"水温38°C 10分钟"）
- 语音支持"刚洗澡了"、"晚上8点洗澡"、"洗澡10分钟"、"洗澡水温38度"

---

### 3.6 身高体重（生长记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| 体重 | 数字输入 | 单位：kg，精确到小数点后1位 |
| 身高/身长 | 数字输入 | 单位：cm，精确到小数点后1位 |
| 头围 | 数字输入 | 单位：cm，可选 |
| 测量日期 | 日期选择器 | 默认当天 |
| 备注 | 文本输入 | 可选 |

**交互说明：**
- 首页快捷操作面板中"身高体重"跳转到独立记录页（与其他类型一致）
- 独立生长页面显示 WHO 标准生长曲线对比图（Canvas 绘制）
- 生长页使用 WHO LMS 参数精确计算百分位，显示为具体百分比（如"17.8%"）
- 曲线图包含 P3/P10/P25/P50/P75/P90/P97 七条百分位参考线
- **曲线图支持双指缩放和单指拖拽**，放大后显示数据点数值
- 支持按体重/身高/头围切换 tab 查看
- 生长页历史记录每条显示各项精确百分位
- 首页时间线中的生长记录同样显示 WHO LMS 精确百分位
- 生长页记录列表参考美柚样式展示：日期/宝宝日龄 + 身高/体重/头围三列 + 百分位状态
- 生长页添加、编辑均跳转到统一记录页，不使用弹窗表单
- 生长页记录卡片右上角"..."提供编辑/删除操作
- 至少填写一项即可保存
- 语音支持单位换算（"5000克"自动转为5kg）

---

## 四、健康模块（体温 & 用药）

### 4.1 模块入口
- 首页快捷操作面板中"健康"按钮（导航到独立健康页）
- 独立健康页含两个 Tab：体温 / 用药

### 4.2 体温记录

| 字段 | 类型 | 说明 |
|------|------|------|
| 体温 | 数字输入 | 单位：°C，范围 34-43 |
| 测量方式 | 选择 | 额温 / 耳温 / 腋温 |
| 测量时间 | 自动 | 记录时间 |
| 备注 | 文本输入 | 可选 |

**交互说明：**
- 健康页顶部显示最近一次体温值+距今时间
- 体温 >= 37.5°C 标黄（低热），>= 38.5°C 标红（高热）
- 最近3天体温历史列表

### 4.3 用药记录

| 字段 | 类型 | 说明 |
|------|------|------|
| 药品名称 | 选择/输入 | **优先展示最近5种不重复药品**，也可手动输入新药 |
| 剂量 | 数字输入 | **选择历史药品时自动带出上次剂量** |
| 剂量单位 | 选择 | ml / 包 / 片 / 滴 / 粒 / 瓶 / g |
| 用药方式 | 选择 | 口服 / 外用 / 雾化 / 塞肛 |
| 备注 | 文本输入 | 可选 |

**交互说明：**
- **历史药品快捷选择**：页面展示最近5种不重复药品卡片
- 点击卡片一键填入药品名+剂量+单位+方式
- 今日用药清单实时展示

### 4.4 每日待办（健康类 P0）

| 字段 | 类型 | 说明 |
|------|------|------|
| 事项类型 | 选择 | 量体温 / 身高体重 / 吃药 / 疫苗 / 自定义健康事项 |
| 事项名称 | 文本/预填 | 用于列表和完成记录展示 |
| 计划时间 | 时间选择器 | 精确到分钟 |
| 重复规则 | 选择 | 每日 / 连续多天 / 每 N 天一次 / 指定日期 |
| 开始/结束日期 | 日期选择器 | 连续多天、每 N 天规则使用 |
| 指定日期 | 日期选择器 | 指定日期规则使用 |
| 用药配置 | 表单 | 药品名、剂量、单位、方式；完成时按配置直接记录 |

**交互说明：**
- 底部 tab 新增「待办」，列表页独立展示每日待办事项。
- 待办新增/编辑使用独立页面 `pages/todo-edit`。
- 完成待办后自动写入正式 `records`，带 `source: 'todo'`、`todoId`、`todoDate`，并进入首页时间线。
- 身高体重待办点击完成时跳转统一记录页填写/保存。
- 删除待办规则只删除 `todos` 配置，不删除已经完成的 `records`。
- 当天已完成但规则已删除的记录会在待办页回填，并提示“规则已删除，记录已保留”。
- 完成项排在未完成项下面，同组按计划时间排序。
- 轮流用药第一版通过“每 N 天一次”实现：药 A 从今天开始每 2 天，药 B 从明天开始每 2 天。
- 右下角新增按钮支持拖动位置，避免遮挡列表内容。
- 小程序在前台时，今日已到点且未完成的待办会震动弹窗提醒；今天过期未完成项打开小程序会补弹。
- 提醒弹窗支持“知道了”和“稍后提醒”。“稍后提醒”会在 10 分钟后再次弹出，并在待办项下方显示倒计时，不修改原待办规则。
- 当前只实现健康类待办；早教类待办未实现。

---

## 五、首页/今日概览

### 5.1 语音快捷入口
- 首页底部固定操作栏放置语音组件
- 左侧为"+"快捷记录入口，中间为语音横条，右侧为键盘输入按钮
- 点击语音横条开始录音，再次点击结束录音
- 点击键盘按钮弹出文字输入框

### 5.2 今日统计卡片
- 今日喂奶次数 & 总奶量
- 今日换尿布次数
- 今日睡眠总时长
- 平均喂奶间隔
- 距离上次喂奶过去了 XX 分钟（每分钟更新）；如果存在进行中的喂奶，则从本次喂奶开始时间计算并显示为“本次喂奶已开始”
- 超过喂奶间隔阈值时标红，阈值在"我的 → 喂养配置"中设置
- 首页顶部显示宝宝名字、按当前筛选日期计算的月龄/天数；切换日期时宝宝天数同步变化
- 今日喂奶计划卡片：显示下一顿、已完成/目标顿数、每顿奶量、夜间勿扰和当天计划时间点；宝宝睡觉中时显示“醒后”而不是硬提醒时间；已喂或开始喂奶后按剩余窗口重新排后续时间
- 喂奶计划到点时，如果小程序在前台，会震动并弹窗提醒；支持“稍后提醒”10 分钟后再弹，首页喂奶计划卡片显示稍后提醒倒计时

### 5.3 进行中状态
- 正在喂奶中... 已用时 XX:XX（点击可跳转去结束）
- 正在睡觉中... 已睡 XX:XX（点击可跳转记录醒来）
- 进行中的喂奶/睡眠卡片提供"取消"按钮，取消后删除该 ongoing 记录，不保存为完成记录

### 5.4 快捷开始
- 今日统计卡片内提供"开始喂奶"和"开始睡觉"两个快捷按钮
- 点击后直接创建对应的进行中记录并开始计时
- 如果已有进行中的喂奶记录，按钮显示"完成喂奶"，点击后按每顿奶量直接结束保存；未设置每顿奶量时跳转到结束页填写
- 如果已有进行中的睡眠记录，按钮显示"宝宝醒了"，点击后直接结束保存
- 语音说"2分钟后开始喂奶"时，首页显示喂奶倒计时；到点自动创建进行中喂奶记录；倒计时期间可取消，也可点快捷按钮立即开始
- 快捷按钮保持紧凑尺寸，但文字、边框、背景、图标视觉更明显；副文案保持短文案

### 5.5 日期导航
- 左右箭头切换前后日期
- 中间显示日期标签（今天/昨天/X月X日）
- 点击日期标签打开 date picker 跳转任意日期
- 非今天时显示"回到今天"按钮
- 切换日期后加载对应日期的记录和统计

### 5.6 时间线视图
- 按时间倒序展示当日所有记录
- 不同类型用不同图标区分：喂奶🍼 / 睡眠💤 / 尿布🧷 / 辅食🥣 / 洗澡🛁 / 体温🌡️ / 用药💊 / 生长📏
- 待办完成生成的疫苗💉 / 自定义健康🏥 记录也进入时间线
- 每条记录右侧有三点菜单（...），点击弹出操作选项
- 记录上显示记录人身份（如"妈妈"）

### 5.7 快捷记录入口
- 首页底部操作栏左侧常驻"+"按钮
- 点击展开七个快捷操作：**喂奶 / 换尿布 / 睡眠 / 洗澡 / 健康 / 身高体重 / 辅食**
- 点击蒙层可关闭面板

### 5.8 程序内提醒
- 提醒范围：今日已到点且未完成的待办/用药、首页喂奶计划中已到点的下一顿。
- 生效条件：小程序在前台运行；后台或关闭小程序时不提醒。
- 到点后默认震动 + 弹窗；今日过期未完成项打开小程序会补弹。
- 弹窗操作：
  - “知道了”：当天该条提醒不再弹。
  - “稍后提醒”：10 分钟后再次提醒。
- 稍后提醒只写入本机本地缓存 `local_reminder_triggered`，不会修改 `todos`、喂奶计划配置或云端数据。
- 待办列表和首页喂奶计划卡片展示稍后提醒倒计时，例如“稍后提醒：10分钟后”。

---

## 六、记录管理（编辑/删除）

### 6.1 编辑记录
- 时间线卡片点击三点菜单 → 选择"编辑"
- 跳转记录页，预填已有数据，所有字段可修改
- 所有类型（含生长记录）均可从时间线进入记录页编辑
- 生长页测量记录列表通过"..."进入同一个记录页编辑
- 进行中的记录（ongoing）点击时跳转计时恢复界面

### 6.2 删除记录
- 三点菜单 → 选择"删除" → 二次确认弹窗
- 时间线卡片支持左滑露出删除按钮
- 生长页测量记录列表也支持删除
- 确认后从数据库删除，刷新时间线

---

## 七、身份与多人协同

### 7.1 身份选择
- 首次使用时自动跳转角色选择页
- 可选角色：爸爸 / 妈妈 / 爷爷 / 奶奶 / 外公 / 外婆 / 其他（可自定义昵称）
- 选择后通过 `familyManage` 云函数注册到 `family_members` 集合

### 7.2 记录标记
- 每条记录自动附带 `recordedBy` 字段（包含 role 和 nickname）
- 时间线上展示记录人身份

### 7.3 家庭成员管理
- "我的"页面 → 家庭成员入口
- **仅妈妈角色可查看和管理**家庭成员列表
- 可修改成员角色

### 7.4 加入方式
- 任何人打开小程序自行选择身份即可使用
- 同一云开发环境下数据共享

---

## 八、数据统计与图表

### 8.1 数据统计（pages/stats，从「更多」进入）
- **当日统计**：喂奶（次数/总奶量/平均间隔/平均耗时）、睡眠（总时长/小睡次数/夜觉时长）、排便（小便/大便/总计）
- **最近7天趋势**：CSS 柱状图展示每日喂奶量、睡眠时长、排便次数；为避免云端大范围查询漏数，按最近 7 个自然日逐日查询后汇总
- 统计页不再放生长曲线入口；生长曲线作为「更多」页独立入口

### 8.2 生长曲线（独立页面 pages/growth）
- 体重/身高/头围 WHO 标准曲线对比图
- 使用 WHO LMS 参数计算精确百分位
- 显示 P3/P10/P25/P50/P75/P90/P97 百分位参考线
- 宝宝实际数据点连线绘制
- **支持双指缩放（最大5倍）和单指拖拽**
- 放大 1.5x+ 时显示数据点数值标签
- 支持 0-24 月龄范围
- 分性别显示（根据宝宝信息中的性别设置）
- 无宝宝生日时按记录顺序均匀分布
- 测量记录列表支持编辑和删除

---

## 九、底部 Tab 栏

| Tab | 页面 | 说明 |
|-----|------|------|
| 首页 | pages/index | 今日概览+时间线+快捷操作 |
| 待办 | pages/todo | 每日健康待办+完成记录 |
| 更多 | pages/more | 记录时间轴+数据统计+生长曲线+QQ音乐 |
| 我的 | pages/mine | 宝宝信息+家庭管理+设置 |

**自定义 tabBar**：使用 `custom-tab-bar` 组件实现。为了降低祖辈日常使用负担，底部只保留高频入口；记录、统计、生长曲线收进「更多」。当前「待办」tab 暂时复用健康图标资源，「更多」tab 使用文字省略号图标，后续可替换正式图标。

---

## 十、页面结构

```
小程序页面结构：
├── 首页 pages/index（语音入口 + 进行中状态 + 统计卡片 + 日期导航 + 时间线 + 快捷操作面板）
├── 待办页 pages/todo（每日健康待办列表 + 完成/未完成分组 + 可拖动新增按钮）
├── 更多页 pages/more（记录时间轴、数据统计、生长曲线、QQ音乐入口）
├── 待办编辑页 pages/todo-edit（健康待办类型、时间、重复规则、用药配置）
├── 记录页 pages/record（各类型记录表单，支持新建/编辑/删除）
│   ├── 喂奶记录（计时/手动/编辑三种模式）
│   ├── 换尿布（类型+形状+颜色+量）
│   ├── 睡眠（计时/编辑）
│   ├── 辅食（食材+量+反应）
│   ├── 洗澡（水温+时长+备注）
│   ├── 体温（温度+方式）
│   ├── 用药（药名+剂量+单位+方式）
│   └── 身高体重（体重+身高+头围+日期）
├── 健康页 pages/health（独立 Tab 式页面）
│   ├── 体温 Tab（记录+历史+状态指示）
│   └── 用药 Tab（记录+快选+今日清单）
├── 生长页 pages/growth（WHO 曲线图+缩放 + 测量记录列表）
├── 统计页 pages/stats（当日/7天趋势，从更多进入）
├── 记录时间轴页 pages/timeline（多日连续倒序时间轴，从更多进入）
├── 我的 pages/mine（宝宝信息+家庭成员+数据导出+关于）
└── 角色选择 pages/role-select（首次进入时跳转）
```

---

## 十一、设置（我的页面）

### 11.1 宝宝信息
- 宝宝昵称
- 出生日期（自动计算月龄/天数）
- 性别（影响 WHO 曲线标准选择）

### 11.2 家庭成员
- 查看已加入的家庭成员（仅妈妈可操作）
- 修改成员角色

### 11.3 喂养配置
- 每顿奶量：可选 90/100/110/120/150ml，也支持自定义；同时用于快捷结束喂奶、语音未识别奶量时的自动填入、首页喂奶计划预估
- 喂奶间隔阈值：可选 1.5/2/2.5/3/3.5/4 小时
- 喂奶计划：可配置每日喝奶顿数、夜间勿扰时间段、最短喂奶间隔；每顿奶量与上方统一配置共用
- AI 参与边界：DeepSeek 后续只能通过 `aiSuggestion` 小幅建议下一顿时间，本地规则仍会校验夜间勿扰、最短间隔、当前时间和最大偏移；当前 UI 默认不启用 AI 自动预测
- 配置存储在云数据库 `config` 集合中，所有家庭成员共享
- 语音记录或结束进行中喂奶时，如果没有奶量，自动填入每顿奶量
- 注意：微信 `wx.showActionSheet` 的 `itemList` 不要超过 6 项；之前「每顿奶量」放了 8 个选项导致弹窗不出现，已改为 5 个常用值 + 自定义

### 11.4 其他
- 程序内提醒默认启用（前台震动+弹窗）；微信订阅消息提醒暂缓
- 数据导出（开发中）
- 关于

---

## 十二、技术备注

### 12.1 微信能力使用
- **微信登录**：通过 OpenID 标识用户
- **录音权限**：`scope.record`，用于语音输入
- **RecorderManager**：录音（mp3/16kHz），上传云存储后调用ASR
- **云开发**：云函数 + 云数据库 + 云存储，数据实时同步

### 12.2 语音识别方案
- 录音 → 云存储 → `speechToText` 云函数 → 腾讯云 ASR（一句话识别）→ 文字
- 文字 → `parseRecord` 云函数 → DeepSeek Chat API → 结构化 JSON
- `speechToText` 云函数超时设置 20 秒，覆盖音频下载与 ASR 调用耗时
- 超时/失败时前端回退到本地正则解析（全类型覆盖）

### 12.3 AI 解析方案
- 云函数 `parseRecord` 调用 DeepSeek Chat API
- System Prompt 定义严格的 JSON Schema 输出格式
- 支持所有事件类型的解析（feeding/diaper/sleep/supplement/bath/health_temp/health_med/growth）
- 支持复杂时间表达式解析（昨天/前天/X号/X分钟前/X分钟后等）
- 云函数超时设置 20 秒，失败时前端回退到本地正则

### 12.4 数据存储
- 云开发数据库为主存储
- 本地 wx.Storage 为降级存储（云不可用时自动切换）
- 药品历史单独维护（`med_history` 集合，去重保留最新5种）
- 共享配置单独维护（`config` 集合，保存每顿奶量、喂奶间隔阈值、喂奶计划每日顿数/勿扰/最短间隔）
- 每顿奶量统一写入新字段 `feedingAmount`，并兼容旧字段 `defaultFeedingAmount` / `feedingPlanAmount`；保存时会同步更新云端已有的多份 config 文档，避免读回旧配置
- 每日待办规则单独维护（`todos` 集合）；完成后生成正式 `records`，删除待办规则不删除历史完成记录
- 云端 `todos` 集合缺失时，待办先使用本地 `local_todos` 降级；集合创建后打开待办页会尝试自动迁移本地规则到云端并保留原 id
- 程序内提醒状态保存在本机 `wx.Storage` 的 `local_reminder_triggered`，用于记录当天已确认或推迟的提醒；不进入云数据库，不改变待办规则
- 本地存储的记录 ID 以 `local_` 前缀区分

### 12.5 程序内提醒方案
- `utils/local-reminders.js` 负责前台提醒调度。
- `app.js` 在 `onShow` 启动提醒循环，在 `onHide` 停止提醒循环。
- 每分钟检查一次今日到点提醒：
  - `todos` 中匹配今日且未完成的待办/用药。
  - `feeding-plan.js` 计算出的下一顿喂奶计划。
- 提醒方式为 `wx.vibrateShort` + `wx.showModal`。
- “稍后提醒”只保存本地临时状态，默认 10 分钟；到期后再次提醒。
- 当前不使用微信订阅消息。已调研一次性订阅消息，但因“一次授权发一次”不适合作为高频喂奶/用药提醒主方案，暂缓实现。

### 12.6 WHO 生长标准数据
- 生长页和首页时间线使用 `utils/growth-standard.js`，内置 WHO LMS 参数
- 通过 LMS 法计算 Z 分数，再用正态分布 CDF 转换为精确百分位
- 支持月龄间线性插值，按天精度计算
- 曲线参考线包含 P3/P10/P25/P50/P75/P90/P97 七条百分位线
- 分男女两套数据，覆盖体重/身长/头围三项指标

### 12.7 配置与密钥管理
- `miniprogram/config.js` 保存云开发环境 ID，对应模板为 `miniprogram/config.example.js`
- `cloudfunctions/speechToText/secret.js` 保存腾讯云 ASR SecretId/SecretKey，对应模板为 `secret.example.js`
- `cloudfunctions/parseRecord/secret.js` 保存 DeepSeek API Key，对应模板为 `secret.example.js`
- `project.config.json` 保存本地微信开发者工具配置和真实 AppID，已加入 `.gitignore`；公开仓库提交 `project.config.example.json`，其中 AppID 使用 `touristappid`
- 真实配置文件已加入 `.gitignore`，对外分发时只提交 `.example.js` 模板
- 如果真实密钥曾经被提交、截图或发给他人，需要在对应平台轮换密钥

---

## 十三、MVP 优先级

### P0 - 已完成（当前版本 v0.8-dev）
1. 喂奶记录（计时+手动，含开始/结束时间）
2. 换尿布记录（形状/颜色/量完整字段，语音支持颜色/形状/量识别）
3. 睡眠记录（计时+详细属性，支持跨天识别）
4. 辅食记录
5. 今日概览/时间线/日期导航/下拉刷新
6. 距上次喂奶计时（可配阈值，绿色/红色指示）
7. 平均喂奶间隔统计
8. 语音快捷记录（腾讯云ASR + DeepSeek LLM + 统一loading + 确认弹窗含编辑按钮）
9. 语音点击切换模式（兼容安卓）+ 键盘文字输入
10. 语音时长智能倒推（"刚喝了奶喝了30分钟"→30分钟前~现在）
11. 健康模块（体温+用药+历史药品快选，语音用药自动写入历史）
12. 身高体重+WHO生长曲线（支持缩放）
13. 记录编辑/删除（三点菜单+左滑删除+语音编辑跳转）
14. 用户身份系统（角色选择+微信昵称+记录标记）
15. 家庭成员管理（妈妈修改其他成员角色，显示微信名）
16. 日期选择器（回溯记录）
17. 数据统计页（当日统计+7天趋势柱状图，从「更多」进入）
18. 自定义 tabBar（大字体统一风格）
19. 统一自定义顶部标题样式 + 底部操作栏整合（+按钮+语音横条）
20. 配置系统（喂奶间隔阈值+每顿奶量）
21. 服务端日期查询（三层降级保障）
22. 配置云端共享（`config` 集合，全家同步每顿奶量和喂奶间隔阈值）
23. 语音开始/结束动作路由修复（start/end/complete 归一化）
24. 生长页 WHO LMS 精确百分位与七条参考线
25. 首页时间线生长百分位迁移到 `growth-standard.js`
26. 密钥外部化（`config.js` / `secret.js` + `.example.js` + `.gitignore`）
27. 延迟开始喂奶（语音未来时间 + 首页倒计时 + 到点自动开始）
28. 洗澡记录（快捷入口 + 统一记录页 + 时间线展示 + 语音解析）
29. 进行中喂奶/睡眠支持修改开始时间并即时重算计时
30. 每日待办 P0（底部 tab + 独立编辑页 + 每日/连续多天/每N天/指定日期 + 完成后生成正式记录）
31. 疫苗/自定义健康记录类型（由待办完成生成，首页时间线与记录页可编辑）
32. 待办规则云端化（`todos` 集合 + 本地降级 + 云端集合创建后迁移）
33. 待办完成记录保留（删除规则不删除已完成 records，完成项排在列表下方）
34. 首页宝宝信息（月龄/天数随日期筛选变化）
35. 首页成功提示明显化（顶部绿色浮层）
36. 首页开始喂奶/开始睡觉快捷按钮明显化（文字缩短、图标放大）
37. 独立记录时间轴页（从「更多」进入；默认两天连续倒序展示，多日图表合并为一张连续卡片，日期标记不单独占行，可继续加载更早日期，有起止时间/跨天记录按时间跨度显示，点状记录只显示图标并避让；卡片简化显示，点击只读详情弹窗）
38. 待办撤销完成（误点完成后可删除当天由待办生成的记录并恢复未完成）
39. 喂奶计划 P0（本地规则计算下一顿与今日计划；配置每日顿数/每顿奶量/夜间勿扰/最短间隔；AI 建议入口受本地规则校验）
40. 底部「更多」页（记录时间轴、数据统计、生长曲线收纳到低频入口）
41. 设置页每顿奶量统一配置（合并默认奶量和计划每顿奶量，修复 actionSheet 选项过多导致无法弹出）
42. QQ 音乐跳转（更多页入口，跳转 QQ 音乐小程序首页）
43. 程序内提醒（前台待办/用药/喂奶到点震动弹窗，过期补弹，稍后提醒，倒计时展示）

### P1 - 待开发
1. 体温趋势折线图
2. tabBar 图标替换为正式设计，尤其是待办 tab 当前仍复用健康图标、更多 tab 当前使用文字省略号图标

### P2 - 后续版本
1. 待办/喂奶后台微信提醒（微信订阅消息 + 云函数定时/服务端调度，当前已暂缓）
2. 早教类待办（最初需求提到但明确可先不做）
3. 数据导出（PDF/图片）
4. 过敏追踪
5. DeepSeek 自动生成喂奶计划建议（调用模型生成 `aiSuggestion`，再由本地规则校验）
6. 里程碑记录
7. 离线同步机制

---

## 十四、2026-05-30 今日需求状态

### 已完成
- 每日待办 P0：新增底部 tab「待办」、列表页和独立编辑页。
- 健康类待办：量体温、身高体重、吃药、疫苗、自定义健康事项。
- 时间规则：具体时间到分钟、每日、连续多天、指定日期、每 N 天一次。
- 轮流用药第一版：用“每 N 天一次”实现 A/B 药交替。
- 待办完成自动写正式记录；疫苗也作为正式健康记录进入时间线。
- 待办配置落 `todos` 集合；每个云环境共享一份家庭数据，避免每个人重复配置。
- 本地待办到云端迁移：云端集合后建时，打开待办页会尝试把本地 `local_todos` 迁移到云端。
- 删除待办规则不删除历史完成记录；待办页当天仍可回填已完成记录。
- 待办支持撤销完成：删除当天由待办生成的正式记录并恢复未完成；规则已删除的归档记录只能删除保留记录。
- 待办 UI：完成项放下面、顶部日期居中、加号可拖动。
- 用药单位新增 `瓶` 和 `g`。
- 首页优化：进行中喂奶时从喂奶开始计算“距上次喂奶”；成功提示更明显；顶部展示宝宝名字/月龄/天数并随日期变化。
- 首页快捷按钮优化：开始喂奶/开始睡觉按钮更明显，副文案减少，图标放大。
- P1 独立记录时间轴：新增 `pages/timeline`，当前从「更多」进入；默认展示最新日期+前一天两天连续倒序时间轴；顶部选日期会重置为该日期+前一天；下拉或滑到底部可继续接上更早日期；多日图表合并为一张连续卡片，日期标记浮在左侧时间轴内不单独占行；喂奶/睡眠/洗澡等时段记录按跨度展示，跨天记录按当天可见区间裁剪；点状记录只显示图标，密集时纵向避让并轻微横向错开；卡片上用药显示药名，其他显示操作名，点击弹出只读详情。
- 底部 tab 从「首页/待办/记录/统计/我的」收敛为「首页/待办/更多/我的」，记录时间轴、数据统计、生长曲线统一放入「更多」。
- 数据统计页最近 7 天改为逐日查询汇总，避免单日有数据但大范围查询漏掉昨天数据。
- 喂奶计划 P0：新增 `utils/feeding-plan.js` 本地规则，首页展示今日计划/下一顿/剩余顿数；「我的 → 喂养配置」可配置每日顿数、每顿奶量、夜间勿扰和最短间隔；AI 建议入口已预留但默认不启用，必须经过本地规则校验。
- 喂养配置合并奶量：原「默认奶量」和「计划每顿奶量」合并为「每顿奶量」，同时服务快捷记录、语音默认奶量和首页预估；修复 `wx.showActionSheet` 选项超过 6 项导致「每顿奶量」点不开的问题。
- QQ 音乐跳转：更多页新增 QQ 音乐入口，使用已验证 appId `wxada7aab80ba27074` 跳转 QQ 音乐小程序首页。
- 程序内提醒：前台运行时对今日到点未完成待办/用药、喂奶计划下一顿进行震动弹窗提醒；今天过期未完成项会补弹。
- 稍后提醒：提醒弹窗支持“稍后提醒”，默认 10 分钟后再次提醒；待办列表和首页喂奶计划卡片显示稍后提醒倒计时。
- 微信订阅消息提醒方案已调研并暂缓：用户最终选择先不用微信服务通知，不创建 `reminders` 集合，不部署 `sendReminders` 云函数。
- 调试日志清理：业务代码中的 `console.log` 已清理，保留 `console.warn/error`。

### 未完成 / 下个 Agent 重点
- P2 待办/喂奶后台微信提醒：订阅消息方案已暂缓，未来如果恢复需重新设计一次性授权任务表。
- 喂奶计划 DeepSeek 自动预测：当前还没有自动调用 DeepSeek 生成 `aiSuggestion`。
- 早教类待办：尚未设计分类、字段、完成记录类型。
- tab 正式图标：待办使用健康图标占位，「更多」当前使用文字省略号图标。
- 微信开发者工具视觉回归：建议重点看程序内提醒/稍后提醒倒计时、记录时间轴、待办页、首页宝宝信息、快捷按钮、成功浮层。

---

*文档版本：v0.8-dev*
*更新日期：2026-05-30*
*更新内容：v0.8-dev - QQ 音乐跳转、程序内待办/用药/喂奶提醒、过期补弹、稍后提醒与倒计时、调试日志清理；微信订阅消息提醒暂缓*
